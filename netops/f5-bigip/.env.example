# BIG-IP 配置查询 Agent

## 环境变量说明

部署时需要配置以下环境变量：

| 变量名 | 必填 | 说明 |
|--------|------|------|
| `ZHIPU_API_KEY` | 是 | 智谱 GLM-4.5 模型的 API Key |
| `ENABLE_LANGFUSE` | 否 | 是否启用 Langfuse 追踪，默认不启用。设为 `true` 启用 |
| `LANGFUSE_PUBLIC_KEY` | 否 | Langfuse 平台的公钥 |
| `LANGFUSE_SECRET_KEY` | 否 | Langfuse 平台的私钥 |
| `LANGFUSE_HOST` | 否 | Langfuse 服务地址，默认为 `https://cloud.langfuse.com` |

### 方式一：.env 文件（本地开发）

在项目根目录创建 `.env` 文件：

```env
ZHIPU_API_KEY=your_api_key_here
ENABLE_LANGFUSE=false
```

### 方式二：Docker 环境变量（生产部署）

```bash
# 方式 A：运行时指定
docker run -e ZHIPU_API_KEY=your_api_key_here -p 5000:5000 bigip-agent

# 方式 B：.env 文件挂载
docker run -v $(pwd)/.env:/app/.env -p 5000:5000 bigip-agent
```

### 方式三：docker-compose.yml（推荐）

```yaml
services:
  bigip-agent:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
```

---

## 快速开始

### 本地开发

```bash
cd d:\我的文档\Codes\lang\deepagents\bigip

# 安装依赖
pip install -r requirements.txt

# 配置 API Key
cp .env.example .env
# 编辑 .env 文件，填入 ZHIPU_API_KEY

# 启动服务
python app.py
```

访问 http://127.0.0.1:5000

### Docker 部署

```bash
cd d:\我的文档\Codes\lang\deepagents\bigip

# 构建镜像
docker build -t bigip-agent .

# 运行容器
docker run -d -p 5000:5000 --name bigip-agent \
  -e ZHIPU_API_KEY=your_api_key_here \
  bigip-agent
```

---

## API Key 获取

智谱 GLM-4.5 API Key 获取地址：https://open.bigmodel.cn/

注册后进入控制台 -> API Keys -> 创建新密钥

---

## 项目结构

```
bigip/
├── app.py              # Flask Web 应用
├── main.py             # LangGraph Agent 定义
├── tools.py            # BIG-IP 配置读取工具
├── bigip.conf          # BIG-IP 配置文件
├── requirements.txt    # Python 依赖
├── Dockerfile          # Docker 构建文件
├── .env.example        # 环境变量示例
├── templates/
│   └── index.html      # Web UI 页面
└── README.md           # 部署说明
```
